<div class="dashboard">

    <main class="content">
        <header>
            <h1>Welcome, Admin!</h1>
        </header>

        <!-- Dashboard Widgets -->
        <section class="widgets1">
            <div class="widget">
                <h3>Total Users</h3>
                <p id="total-users">5</p>
            </div>
            <div class="widget">
                <h3>Total Books</h3>
                <p id="total-books">12</p>
            </div>
        </section>
            <section class="widgets1">
            <div class="widget">
                <h3>Total Orders</h3>
                <p id="total-orders">4</p>
            </div>
            <div class="widget">
                <h3>Total coupons</h3>
                <p id="total-coupons">3</p>
            </div>
        </section>
    </main>
</div>
<section class="suggestions">
    <h2>Book Suggestions</h2>
    <table>
        <thead>
            <tr>
                <th>Title</th>
                <th>Author</th>
                <th>Date</th>
            </tr>
        </thead>
        <tbody>
            <% suggestions.forEach(suggestion => { %>
                <tr>
                    <td><%= suggestion.title %></td>
                    <td><%= suggestion.author %></td>
                    <td><%= new Date(suggestion.date).toLocaleDateString() %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</section>


<script>
// scripts/dashboard.js
document.addEventListener('DOMContentLoaded', function() {
    fetch('/api/dashboard-stats')
        .then(response => response.json())
        .then(data => {
            document.getElementById('total-users').textContent = data.totalUsers;
            document.getElementById('total-books').textContent = data.totalBooks;
            document.getElementById('total-orders').textContent = data.totalOrders;

            const recentActivityList = document.getElementById('recent-activity-list');
            data.recentActivity.forEach(activity => {
                const li = document.createElement('li');
                li.textContent = `${activity.timestamp} - ${activity.description}`;
                recentActivityList.appendChild(li);
            });
        })
        .catch(error => console.error('Error fetching dashboard data:', error));
});
</script>

<style>
    body, html{
        margin: 0;
        padding: 0;
        height: 100%;
    }

    body {
    margin: 0;
    font-family: Arial, sans-serif;
    background-color: #f4f4f4;
}

.dashboard {
    display: flex;
}

.content {
    margin-left: 250px;
    padding: 20px;
    flex: 1;
}

.widgets1 {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); /* Responsive grid with a minimum width of 200px for each widget */
    gap: 20px; /* Space between widgets */
    padding: 20px;
}

/* Individual widget styling */
.widget {
    background-color: #fff; /* White background */
    border-radius: 8px; /* Rounded corners */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    padding: 20px;
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

/* Hover effect for widgets */
.widget:hover {
    transform: translateY(-5px); /* Slight lift on hover */
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.15); /* Enhanced shadow */
}

/* Styling for widget headings */
.widget h3 {
    margin-top: 0;
    color: #333; /* Dark text color */
    font-size: 18px;
}

/* Styling for widget stats */
.widget p {
    font-size: 24px;
    font-weight: bold;
    margin: 10px 0 0; /* Margin only at top and bottom */
    color: #007bff; /* Primary color */
}
</style>